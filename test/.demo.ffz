/*#V3: struct {x: u64, y: u64, z: u64}

#add_vectors: proc(a: V3, b: V3) => (V3) {
	ret V3{a.x + b.x, a.y + b.y, a.z + b.z}
}*/

#ffz_entry: proc() {
	/*a: V3{55, 22, 11}
	b: V3{1, 2, 5}
	c: add_vectors(a,b)
	*/
	
	v: [?]u32{1, 2, 3, 4, 5}
	second: v[1]
	
	dbgbreak
}

//@link_library: ".ffz/c_things.obj"
//#receive_v3: @extern proc(aa: V3)

// This program works if you set FIX_TB_BIG_RETURN_HACK to 1, but will segfault if you set it to 0.
